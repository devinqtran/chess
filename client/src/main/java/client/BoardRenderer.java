package client;

public class BoardRenderer {
    public static void render(boolean whitePerspective) {
        System.out.println("Board rendering not yet implemented!");
        System.out.println("Perspective: " + (whitePerspective ? "WHITE" : "BLACK"));
    }
}